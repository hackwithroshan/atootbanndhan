// The API_URL is now an empty string for all environments.
// In development, Vite's proxy will forward requests from '/api' to 'http://localhost:5000'.
// In production, Vercel's rewrites will handle routing '/api' to the backend function.
export const API_URL = '';